# 保管領域

役目を終えた文脈ファイルの置き場です。

**このディレクトリはエージェントの参照対象から明示的に外します**。削除ではなく退避にするのは、後から経緯をたどれるようにするためです。

参照対象に残したまま「古い」と注記する方式は採りません。注記は読まれず、古い記述がそのまま使われます。

## 移すもの

- 終了した案件の案件層ファイル
- 置き換えられた設計標準
- 使われなくなった用語(用語集からは削除し、ここへ経緯を残す)

## 移さないもの

- 判断記録(ADR)。ステータスを「廃止」または「置き換え」にして `../decisions/` に残す
