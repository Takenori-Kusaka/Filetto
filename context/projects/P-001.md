---
owner: takenori-kusaka
scope: '**'
---

# P-001 Filetto

**最終更新: 2026-08-12** / 現在地: **S1 価値確立**(SG-0 通過済み)

## 案件の目的

個人が自分の PC に 1 コマンド(`docker compose up`)で立てられ、**ファイルを置いた瞬間から人にも AI にも検索可能になる**、課金なしのファイル倉庫を作る。

人向けの入口は Windows Explorer / Google Drive 相当の**ファイル操作が一級市民**の UI。AI 向けの入口は同一のファイル資産に対する**セマンティック検索と MCP アクセス**。この 2 つが同じ実体を共有していることが本プロダクトの価値であり、既存の RAG 製品との差である。既存製品はいずれも「取り込み口」であってエクスプローラーではない。

探した先でやることは **RAG** である。**RAG に取り込めないなら、見つけても意味がない。** これが検索精度とは別に、抽出テキストの質を問う理由である。**この問いは S0-004 で実測し、成立を確認した。**

Filetto 完了後に Chat / Auth を別マイクロサービスとして開発し、Docker アーキテクチャへ合成する。最終形は「AI Editor Framework(NotebookLM リスペクト)」。

## 費用に関する事業上の前提

**費用の最小化は、訴求の言い換えではなく目的そのものである。**

同種の機能は有償 SaaS として既に存在する。**費用を出せるならそちらでよい**というのが利用者の合理的判断であり、「一定以上の費用が掛かり、採算性の面で有償 SaaS より良い選択肢にならない」時点で、**本 OSS の経営層観点での差別化は消滅する**。技術者観点(組み込み容易性・拡張性・自由度)での優位は残りうるが、それだけでは中核訴求を支えられない。

ただし **「ゼロ」なのは金銭だけ**である。S0-003 の実測では 50 ファイルの初回構築に約 25 分、モデルのディスク使用量は約 1.04 GB(S0-004 の推奨構成)。数千ファイルなら数時間から十数時間。**企画書 v2.0 では「無料」ではなく「課金なし・自分のマシンで完結」と書く。**

運用時 $0・実行時の外部通信なし・API キー不要は S0-004 が実測で確認した。**金銭の主張は維持し、時間とディスクの主張はしない。**

## 対象とする利用者

**日本国内の利用者と日本語文書を対象とする。**

この前提は技術選定に対する拘束力を持つ。日本語の文書に対して中国語字形を返す OCR は、法務・引用という特定用途の制約ではなく**中核体験そのものの欠陥**として扱う。「読める」と「正しく読める」を分けて売らない([#12](https://github.com/Takenori-Kusaka/Filetto/issues/12))。

## 現在地とゲートの通過状況

| ゲート | 状態 | 記録 |
| --- | --- | --- |
| D-0 体制図 | **承認済み v1.1** | `docs/D-0-governance.md` |
| G-1 企画承認 | **通過** | `docs/gates/g1-P-001-2026-08-12.md` |
| SG-0 探索完了 | **S1 へ移行** | `docs/gates/sg0-P-001-2026-08-12.md` |
| G-2 要件合意 | **未着手。次のマイルストーン** | [#17](https://github.com/Takenori-Kusaka/Filetto/issues/17) |
| G-3 技術設計判断 | 未着手(ADR は先行して4本採用済み) | — |
| G-4 機能仕様承認 | 未着手 | — |
| G-5 自動検証 | 稼働中。`main` で全11ジョブ緑 | — |
| G-6 独立レビュー | **未達**(省略ではない) | `PROCESS-PROFILE.md` |
| G-7 / G-8 | 未着手 | — |

## 次のマイルストーン

**G-2 要件合意**(価値責任者・48時間)。判定基準は次の5点。

| # | 判定基準 |
| --- | --- |
| 1 | 受入基準が検証可能な形式(EARS 記法)で書かれている |
| 2 | 曖昧語が残っていない |
| 3 | **暗黙の前提が案件層コンテキストに登録済み** |
| 4 | スコープ外事項が明記されている |
| 5 | **合意した範囲がスコープ3層(SC-M / SC-P / SC-B)へ割り当てられている** |

基準5 で割り当てた層が、G-4 における範囲内外の判定の基準になる。**G-2 で層を割り当てていない案件では G-4 の判定が成立しない。**

### G-2 に入る前に片付けるもの

| Issue | 作業 | 状態 |
| --- | --- | --- |
| [#12](https://github.com/Takenori-Kusaka/Filetto/issues/12) | OCR 経路の再選定 | **完了**。[S0-006](../../docs/spikes/S0-006-ocr-glyph.md)。既定 + 字形正規化で混入 0・速度そのまま |
| [#13](https://github.com/Takenori-Kusaka/Filetto/issues/13) | Lore の採用 | **完了**。[[0008-lore-as-versionstore]] |
| [#14](https://github.com/Takenori-Kusaka/Filetto/issues/14) | **ADR-0002 対外インターフェース契約** | **未着手**。`state:needs-dev` |
| [#15](https://github.com/Takenori-Kusaka/Filetto/issues/15) | ADR-0005 インデクサ選定 | **完了**。[[0005-indexer-selection]] 採用 |
| [#16](https://github.com/Takenori-Kusaka/Filetto/issues/16) | 企画書 v2.0 | **一部完了**。`docs/project-brief.md` を v2.0 へ改訂済み。**観点7 チャネルの3欄が未記入** |
| [#17](https://github.com/Takenori-Kusaka/Filetto/issues/17) | **G-2 要件合意** | **判定待ち**。`specs/F-001` 〜 `specs/F-003` に受入基準76件を起草済み |

**残るのは #14 と、#16 の観点7、そして #17 の判定である。**

**#14 は F-003 の前提1 でもある。** ADR-0002 が承認されるまで F-003 の受入基準1〜21 は確定しない。**F-001 と F-002 のみ先に判定し、F-003 を ADR-0002 の後へ回す分割を選べる。**

ADR-0004(JWT クレーム契約)は G-3 の判定対象だが、F-003 の基準24〜27 に影響する。

**#12 は本日新たに発生したブロッカーである。** S0-004 は RapidOCR を推奨したが、対象利用者の前提(上記)により中核構成の層A が再度未確定となった。

## PO が下した判断

### 1. 中核構成は案B(2026-08-12 決定)

パース + BM25 / e5-small を中核に置き、LLM はオプション層に置く。

| 案 | 採否 | 理由 |
| --- | --- | --- |
| 案A パース + BM25 のみ | 不採用 | S0-003 でハイブリッド(3:7)が 10/10。埋め込みを外す理由がない |
| **案B 案A + LLM をオプション層** | **採用** | 運用時 $0・外部通信なしを保ったまま、LLM を持つ利用者だけが上積みを得る |
| 案C GraphRAG 系 | 不採用 | LLM 必須で費用の前提と衝突する。S0-001 で Graphify の実用精度も届かない |

層B・層C は確定。**層A(パース)の OCR のみ #12 待ち。** `.xlsx` は ExStruct(`--mode light`)で確定(S0-004 で 3/3)。

### 2. Lore を VersionStore に採用する(2026-08-12 決定)

**自前 CAS は採らない。履歴管理を独自実装することは技術負債にしかならない。**

[[extensibility]] は最優先事項として拡張性を置いている。履歴管理・重複排除・破損検出を自前で持つと、実装した瞬間から永続的に保守対象となり、**部品として将来的に差し替える余地を自ら潰す**。この観点で自前 CAS は強く否定される。S0-005 が挙げた「`loreserver` の常駐(コンテナ +1)」という費用は、独自実装の保守費用より小さい。

S0-005 の4条件をすべて受け入れる。

| # | 条件 |
| --- | --- |
| 1 | `loreserver` の常駐を受け入れる(`docker compose` に1コンテナ追加) |
| 2 | 保管パスを明示設定する(既定は一時ディレクトリで消える) |
| 3 | 保管領域の定期検証と `.~loretemp` の掃除を Filetto 側で持つ |
| 4 | `VersionStore` 境界の背後に置く(0.x の間は API が非互換に変わりうる) |

**条件4 は条件1 の帰結でもある。** 境界を置く以上、将来 Lore を別実装へ差し替える経路は残る。これが自前 CAS との決定的な差である。

### 3. ADR-0002 の4論点(判断取得済み・執筆は [#14](https://github.com/Takenori-Kusaka/Filetto/issues/14))

| 論点 | 決定 |
| --- | --- |
| 外部が指す識別子 | **安定 ID**。パスは属性にすぎない |
| ファイル内容の受け渡し | **本文直返し + 署名付き URL の両方** |
| MCP の提供形態 | **HTTP を主、stdio も提供** |
| 初回構築中の扱い | **候補に含めず、未完了 N 件を明示する** |

## 暗黙の前提(G-2 で正式登録する)

| # | 前提 | 状態 |
| --- | --- | --- |
| 1 | ~~Graphify が汎用ドキュメントで実用精度を出す~~ | **解消**。中核に据えない([S0-001](../../docs/spikes/S0-001-graphify.md)) |
| 2 | 外部 API なしで実用的なセマンティック検索が成立する | **成立**([S0-003](../../docs/spikes/S0-003-parser-and-hybrid.md) / [S0-004](../../docs/spikes/S0-004-rag-fitness.md)) |
| 3 | 抽出テキストが RAG の文脈として使える | **成立**。[S0-004](../../docs/spikes/S0-004-rag-fitness.md) で 15/15(誤答0・回答不能0) |
| 4 | 利用者は Docker を起動できる | 未検証。「非エンジニアに広げる」目的と衝突しうる。**Lore 採用でコンテナが1個増えた** |
| 5 | AGPL-3.0 でも個人セルフホストの採用は妨げられない | 未検証。崩れたら [[0001-license-agpl-3]] を見直す |
| 6 | 初回構築の所要時間(数千件で数時間)を利用者が受け入れる | **未検証。企画の訴求に直結する** |
| 7 | 日本語文書の OCR で中国語字形の混入を 0 にできる | **未検証。[#12](https://github.com/Takenori-Kusaka/Filetto/issues/12) で測定中。層A の確定条件** |

## 決まっていないこと

| # | 未決の論点 | 期限 |
| --- | --- | --- |
| 0 | ~~中核の検索構成~~ | **決定済み。案B**(上記) |
| 1 | ~~Lore の採否~~ | **決定済み。採用**(上記) |
| 2 | インデクサ抽象化インターフェースの具体形 | G-3 まで |
| 3 | JWT クレームの設計(クレーム名・スコープの表現・検証の責務分界) | ADR-0004 |
| 4 | フロントエンドの検査を G-5 へどう組み込むか。**アダプタが単一値のため標準経路では回らない** | 実装着手前 |
| 5 | コンテナ構成(単一イメージか複数か)。**Lore 採用により +1 コンテナが確定** | ADR-0002([#14](https://github.com/Takenori-Kusaka/Filetto/issues/14)) |
| 6 | 市場・顧客仮説の検証の未達をどう埋めるか。調達先は G-6 と同じ「公開してコミュニティの反応を観測する」であり、**片方が埋まらなければもう片方も埋まらない構造** | 2026-11-12 |
| 7 | チャネル(どう知ってもらうか)。標準に欄が存在しない。AGPL は採用の裾野を狭めるため論点6と掛け算になる | 企画書 v2.0([#16](https://github.com/Takenori-Kusaka/Filetto/issues/16)) |
| 8 | ミラーディレクトリの原本との対応の取り方。S0-004 が**内容ハッシュを主キーとする構成**を提案済み。対外の安定 ID との関係が未決 | ADR-0002([#14](https://github.com/Takenori-Kusaka/Filetto/issues/14)) |
| 9 | **日本語文書で中国語字形を出さない OCR 経路** | [#12](https://github.com/Takenori-Kusaka/Filetto/issues/12) |

## 判断記録(ADR)

| ID | 内容 | 状態 |
| --- | --- | --- |
| [[0001-license-agpl-3]] | ライセンスを AGPL-3.0 とする | 採用 |
| [[0003-reversible-file-operations]] | 破壊的操作をすべて可逆にする。ゴミ箱30日・自動削除・Ambient 通知 | 採用。**決定5 の初版実装は [[0008-lore-as-versionstore]] が置き換えた** |
| [[0005-indexer-selection]] | 中核の検索構成を案B とする。層A は既定 OCR + 字形正規化 | **採用** |
| [[0006-implementation-stack]] | Python バックエンド + TypeScript フロント | 採用 |
| [[0007-allowed-licenses]] | 許可ライセンス一覧の補正。**暫定部分は pit-in-template#13 修正後に削除** | 採用(一部暫定) |
| [[0008-lore-as-versionstore]] | VersionStore の実装に Lore を採用し、自前 CAS を採らない | **採用** |
| ADR-0002 | 対外インターフェース契約(REST / MCP) | **未執筆([#14](https://github.com/Takenori-Kusaka/Filetto/issues/14))。判断は取得済み。F-003 の前提** |
| ADR-0004 | JWT クレーム契約 | 未執筆。F-003 の基準24〜27 に影響する |

## 恒久層の設計標準

| 標準 | 要点 |
| --- | --- |
| [[calm-technology]] | アテンションを必要最小限しか奪わない。通知3層(Ambient / Status Tones / Status Shouts)。**「今は静かだが後で叫ぶ」設計を作らない** |
| [[extensibility]] | **「MVP だからカスタマイズ性を考慮しない」は認めない**。運用に依存する値を定数にしない。差し替わる実装は境界の背後に置く |

## S0 探索の記録

| ID | 内容 | 結論 |
| --- | --- | --- |
| [S0-001](../../docs/spikes/S0-001-graphify.md) | Graphify の実用精度 | スキル経由なら 8/10 だが LLM 必須。**中核に据えない** |
| [S0-002](../../docs/spikes/S0-002-alternatives.md) | 代替案の実測 | パース + BM25 で 8/10 / 31秒 / $0 |
| [S0-003](../../docs/spikes/S0-003-parser-and-hybrid.md) | Docling / ハイブリッド検索 / 神エクセル | 検索 10/10 / $0 / 外部通信なし。**推奨構成は S0-004 が差し替え** |
| [S0-004](../../docs/spikes/S0-004-rag-fitness.md) | RAG 適合性で測り直す | **15/15(誤答0・回答不能0)**。中核構成を差し替え |
| [S0-005](../../docs/spikes/S0-005-lore.md) | Lore の採用可否 | 条件つきで採用できる。**PO は採用を決定** |
| [#12](https://github.com/Takenori-Kusaka/Filetto/issues/12) | 中国語字形が混入しない OCR 経路 | **実施中** |

### S0-004 が S0-003 を覆した点

| S0-003 の記述 | S0-004 の実測 |
| --- | --- |
| OCR は EasyOCR(ja) を推奨 | **スキャン PDF 4問すべて回答不能**。RapidOCR 全ページで 4/4 正答・回収率 82%/53%・4.4 倍速い |
| 神エクセルの列見出しとの交差はどのパーサも復元できない | **ExStruct で復元できる**(3/3)。近接判定という測り方の限界だった |
| 「スキャン PDF は探せるが読めない」 | **取り下げ。** 差はエンジンであって設定ではない(対照実験で確認) |

**検索では 10/10 だった構成が、RAG 適合性では 15問中 4問しか正答できなかった。探せることと使えることは別だった。**

### S0 で判明した制約

| 項目 | 事実 |
| --- | --- |
| 初回構築 | 50 ファイルで約 25 分(Docling)。純 Python パースの約58倍 |
| モデル | 約 1.04 GB のディスク(docling-models 342MB + layout-heron 164MB + RapidOCR 62MB + e5-small 471MB) |
| 誤答 | 「項目名は正しいが項番が違う」「隣の項目の選択肢を拾う」という**気づきにくい誤り**が実在する。最終構成では 0 だが、評価セットを増やせば再び出る |
| 視覚ベース検索 | **成立しない。** ローカル CPU で 16.7 秒/ページ・日本語 2/5。ColPali v1.3 の base は **gemma ライセンス**で 5.8GB |
| Docling VLM | RapidOCR と同じ 8/8 に達するが **15.4 倍遅い**(270 秒/ページ)。予備手段としては残す価値がある |
| MarkItDown | スキャン PDF・画像に対して **0 文字**。ローカル完結の制約下では使えない |
| Docling と Excel | 結合セルを全スパンに複製展開し、出力が 76 倍に膨張する。**Excel には向かない** |
| YomiToku | **CC BY-NC-SA 4.0** で商用不可 |

## 標準・テンプレートへ返した論点

| 先 | 番号 | 内容 | 状態 |
| --- | --- | --- | --- |
| process-compass | #208 | 軸E: 設計変更で危害の帰結を下げる選択肢がガイダンスにない | Open |
| process-compass | #209 | D-0 の兼務不可と `approverMode=value-owner-merged` の衝突 | Open |
| process-compass | #210 | 企画段階のコア前提検証 | **取り下げ**(標準に S0/SG-0 として既に規定があった) |
| pit-in-template | #2 | 生成物に S0 / SG-0 への導線がない | Open |
| pit-in-template | #3 | 調達先の記入導線がない。**再実行で記入が消える** | Open |
| pit-in-template | #4 | 企画書テンプレが標準テンプレ6 と乖離 | Open |
| pit-in-template | #6 | `setup-stack` が `install: 'false'` を無視する | Open |
| pit-in-template | #12 | `python.json` の `secretScan` が空 | Open |
| pit-in-template | #13 | `ip-clearance` が実害なく失敗する | Open |

## 運用上の注意(次のセッションへ)

1. **`secret-scan` は実行されていない。** `adapters/python.json` の `secretScan` が空のため「実施しません」で通過している。**実装着手までに埋める**(pit-in-template#12)
2. **`allowedLicenses` に暫定の回避策が入っている。** pit-in-template#13 の修正後に表記吸収分を削除する。確認は D-0 の次回レビュー期日(2026-11-12)
3. **強制層で保護されたファイルはエージェントから書けない。** `process.config.json` / `PROCESS-PROFILE.md` / `adapters/**` / `.github/workflows/**` / `.claude/**`。**Bash 経由での迂回はしない**
4. **`gh pr merge` と `gh pr review` もエージェントには拒否される。** マージは人の操作
5. **G-6 が未達である以上、タスクの粒度が品質そのものになる。** 1タスク100行目安・上限400行・変更ファイル15以内を機械的に守る
6. **テストは実装より先に生成し、テスト生成に実装コードを渡さない**(5.8.4 の独立性「中」)
7. **PR のベースブランチを `main` 以外にしない。** PR #6 のベースが `docs/s0-001-graphify-spike` で、そのブランチを取り込む PR #3 が先にマージされていたため、**S0-003 の記録が `main` へ到達していなかった**。PR #11 で復旧済み。トランクベースの前提であり、ベースは常に `main` とする
8. **受信箱が全ロールで空のとき、それは「捌けている」ではなく「起票されていない」を疑う。** 2026-08-12 時点で Open な Issue / PR が 0 件になったが、原因は次の6件が未起票だったことだった
