# エージェント向けの入口

このプロジェクトは**ピットイン方式**(正式名称: AI協調型ソフトウェア開発プロセス標準)で開発します。

開発規約の正本は [`CLAUDE.md`](./CLAUDE.md) です。エージェントの種類にかかわらず、同じ規約に従ってください。

## 最低限これだけは

1. `PROCESS-PROFILE.md` と `process.config.json` を読む。有効なゲートはここが正本
2. 受入基準が確定していない機能の実装を始めない
3. **テストの失敗を、テスト側の変更で解消しない**
4. 独立レビューの挙動要約を生成しない
5. カバレッジ閾値・静的解析の重大度・除外設定を、機能変更と同じ変更単位で触らない
6. 結果責任は人に紐づく。承認・判定を代行しない

## 遮断されている操作

`.claude/settings.json` が書き込み範囲・ネットワーク・コマンドの許可範囲を定めています。範囲外は既定で拒否されます。**許可範囲の変更をエージェントが行ってはなりません**。

拒否された操作は記録に残り、確認の対象になります。拒否の発生そのものが、タスクの範囲設定と実態が合っていない兆候です。

## 詳細

[`CLAUDE.md`](./CLAUDE.md) を読んでください。
